#include<iostream>
#include<vector>
#include<list>
#include<algorithm>
using namespace std;

main()
{
    int n;cin>>n;
    while(n--)
    {
    int m,d;
    cin>>m;
    vector<int>v(m);
    for(int i=0;i<m;i++)
    cin>>v[i];
    cin>>d;
    reverse(v.begin(),v.begin()+d);
    reverse(v.begin()+d,v.end());
    reverse(v.begin(),v.end());
    for(int i=0;i<m;i++)
    cout<<v[i]<<" ";
    cout<<endl;
        
    }
}
