#include<iostream>
#include<vector>
#include<list>
#include<algorithm>
using namespace std;
main()
{
    int n;cin>>n;
    while(n--)
    {
    int m,x=0;
    cin>>m;
    vector<int>v(m);
    for(int i=0;i<m;i++)
    {
        cin>>v[i];
        
    }
    for(int i=0;i<v.size();i++)
    {
        x=x^v[i];
    }
        cout<<x<<endl;
    }
}
